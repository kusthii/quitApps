#!/usr/bin/env bash

DIR=${INIT_CWD:-"."}
BIN_DIR="${DIR}/node_modules/@raycast/api/bin"
ARCH_NAME="$(uname -m)"
ARCH_DIR="x86"

if [ "${ARCH_NAME}" = "arm64" ]; then
  ARCH_DIR="arm64"
fi

"${BIN_DIR}/${ARCH_DIR}/ray" "$@"
